/**
* @flow
* {{ properCase name }}
*/
import React from 'react';
// import styled from 'styled-components';

// define prop types
type {{ camelCase name }}Props = {
}

class {{ properCase name }} extends React.PureComponent { // eslint-disable-line react/prefer-stateless-function
  props: {{ camelCase name }}Props

  // define state types
  state: {
    // open: boolean,
  }

  constructor(props: {{ camelCase name }}Props) {
    super(props);

    this.state = {
      // open: false,
    };
  }

  render() {
    return (
      <div>
      </div>
    );
  }
}

export default {{ properCase name }};
